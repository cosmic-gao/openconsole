# Unreal Blueprint 深度解析

> 工业级可视化脚本系统 | 图灵完备 | 字节码编译 | K2Node 扩展机制

---

## 1. 概述与定位

Unreal Blueprint（蓝图）是 Epic Games 在 2014 年随 UE4 推出的可视化脚本系统。其设计目标是：

1. **让设计师能编程**：不写 C++ 代码即可实现游戏逻辑
2. **与 C++ 无缝互通**：`UFUNCTION(BlueprintCallable)` 即可暴露原生函数
3. **真正编译执行**：不是解释执行，而是编译为字节码在专用 VM 上运行
4. **图灵完备**：变量、函数、宏、循环、继承等全部支持

**重要区分**：Blueprint ≠ Kismet（UE3 的可视化脚本，已废弃）。Blueprint 是全新设计的系统，复用了 Tim Sweeney 在 1998 年为 UnrealScript 编写的 VM。

---

## 2. 核心架构

### 2.1 五大类层次

```
UBlueprint (UObject)
  │
  ├── UEdGraph          // 可视化编辑器中的图
  │     ├── UEdGraphNode[]           // 节点基类
  │     │     └── UK2Node            // Blueprint 专用节点基类
  │     │           ├── UK2Node_CallFunction    // 调用 C++ 函数
  │     │           ├── UK2Node_IfThenElse     // 条件分支
  │     │           ├── UK2Node_ForEach        // 循环
  │     │           ├── UK2Node_SpawnActor     // 生成 Actor
  │     │           └── ...                    // 100+ 内置类型
  │     └── UEdGraphPin[]           // 输入/输出引脚
  │
  └── UBlueprintGeneratedClass  // 编译后的真正 UClass
        └── UFunction
              └── Script: TArray<uint8>  // 字节码数组
```

### 2.2 Pin（引脚）系统

Pin 是 Blueprint 最核心的概念，分为两类：

| 类型 | 颜色 | 含义 |
|------|------|------|
| **Exec Pin** | 白色 | 控制流，执行顺序 |
| **Data Pin** | 彩色 | 数据，按类型着色 |

Data Pin 类型与颜色对应：

| 类型 | 颜色 | 示例 |
|------|------|------|
| Boolean | 绿色 | `true` / `false` |
| Integer | 红色 | `42` |
| Float | 橙色 | `3.14` |
| String | 黄色 | `"Hello"` |
| Object | 蓝色 | `APawn*` |
| Class | 紫色 | `ACharacter::StaticClass()` |

---

## 3. 数据模型详解

### 3.1 UEdGraphPin 结构

```cpp
// 简化自 Engine/Source/Editor/BlueprintGraph/Classes/KismetNodes/
struct FEdGraphPinType {
    FName PinCategory;           // "Exec" / "Boolean" / "Object" 等
    FName PinSubCategory;        // 子类别，如 "Integer" 的 "int"
    UObject* PinSubCategoryObject; // 对象引用，如 SpecificClass
    EPinContainerType ContainerType; // None / Array / Map / Set
    bool bIsArray;              // 是否数组
    bool bIsMap;                // 是否 Map
};

class UEdGraphPin : public UObject {
    FString PinId;               // 唯一标识 (GUID)
    FName PinName;              // 引脚名称

    EEdGraphPinDirection Direction;  // EGPD_Input / EGPD_Output

    // 连接到本 Pin 的其他 Pin
    TArray<UEdGraphPin*> LinkedTo;

    // 类型信息
    FEdGraphPinType PinType;

    // 默认值（用于常量输入）
    FString DefaultValue;        // "true", "42", "MyActor" 等
    FString AutogeneratedDefaultValue; // 自动生成的默认值

    // 关联的节点
    UEdGraphNode* ParentPin;     // 父 Pin（用于数组元素的子 Pin）
    UEdGraphNode* GetOwningNode() const;
};
```

### 3.2 UK2Node 结构

```cpp
// 简化自 Engine/Source/Editor/BlueprintGraph/Classes/K2Node/
class UK2Node : public UEdGraphNode {
    // 本节点的所有 Pin
    TArray<UEdGraphPin*> Pins;

    // 节点类别（用于编辑器分类）
    FName NodeCategory;          // "Events" / "Flow Control" / "Math" 等
    FName MenuDescription;      // 鼠标悬停显示

    // 展开函数 - 核心！编译时调用
    // 把本节点展开为一个或多个底层节点
    virtual void ExpandNode(
        FKismetCompilerContext& CompilerContext,
        UEdGraph* SourceGraph
    ) {}

    // 获取本节点的函数引用（对于调用函数的节点）
    FMemberReference FunctionReference;
};
```

### 3.3 Pure vs Impure 节点

这是 Blueprint 最重要的设计之一：

```cpp
// Pure 节点 - 无 exec pin，无副作用，按需求值（类似数据流）
// 特征：UK2Node::IsPure() == true
// 用途：数学运算、类型转换、Getter

// Impure 节点 - 有 exec pin，有副作用，按执行顺序运行（类似控制流）
// 特征：UK2Node::IsPure() == false
// 用途：Spawn Actor、Print String、Set Variable
```

---

## 4. 编译管线深度解析

这是 Blueprint **最精华也最复杂的部分**。它不是"图解释器"，而是"图编译器"。

### 4.1 编译流程图

```
┌─────────────────────────────────────────────────────────────────┐
│  UEdGraph (编辑器中的可视化图)                                      │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │ 阶段 0: Validation - 检查基础连通性                          ││
│  └──────────────────────────────────────────────────────────────┘│
│                            │                                      │
│                            ▼                                      │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │ 阶段 1: ExpandNode                                           ││
│  │           │                                                  ││
│  │           ├── K2Node_SpawnActorFromClass                    ││
│  │           │     ↓ ExpandNode()                               ││
│  │           ├── BeginDeferredActorSpawnFromClass               ││
│  │           ├── [ExposeOnSpawn 变量的 Setter]                  ││
│  │           └── FinishSpawningActor                            ││
│  │                                                                  ││
│  │           K2Node_MacroInstance                                ││
│  │     ↓ ExpandNode()                                            ││
│  │     展开为宏定义中的所有节点                                     ││
│  │                                                                  ││
│  │           K2Node_IfThenElse                                    ││
│  │     ↓ ExpandNode()                                            ││
│  │     展开为 Branch + 两条执行线                                  ││
│  └──────────────────────────────────────────────────────────────┘│
│                            │                                      │
│                            ▼                                      │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │ 阶段 2: 拓扑排序 → 线性字节码序列                               ││
│  │                                                                  ││
│  │  即使图中有循环，也生成线性序列，通过 JUMP 指令实现循环          ││
│  └──────────────────────────────────────────────────────────────┘│
│                            │                                      │
│                            ▼                                      │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │ 阶段 3: FNodeHandlingFunctor 生成中间表示 (IR)                ││
│  │                                                                  ││
│  │  每种节点类型配套一个 Functor：                                  ││
│  │     ├── FKCHandle_CallFunction    → 处理函数调用              ││
│  │     ├── FKCHandle_VariableSet     → 处理变量赋值               ││
│  │     ├── FKCHandle_Branch          → 处理条件分支               ││
│  │     └── ...                                                    ││
│  │                                                                  ││
│  │  生成 FBlueprintCompiledStatement 序列：                       ││
│  │     Statement { Op, Destination, Sources[], ... }             ││
│  └──────────────────────────────────────────────────────────────┘│
│                            │                                      │
│                            ▼                                      │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │ 阶段 4: FScriptBuilderBase 序列化字节码                       ││
│  │                                                                  ││
│  │  将 IR 转换为 EExprToken 枚举值，写入 UFunction::Script       ││
│  └──────────────────────────────────────────────────────────────┘│
│                            │                                      │
│                            ▼                                      │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │ 阶段 5: 写入 UBlueprintGeneratedClass，编译完成               ││
│  └──────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 字节码指令系统

字节码是单字节枚举（因此叫 byte code），常用指令：

```cpp
// 简化自 Engine/Source/Runtime/Engine/Classes/Kismet/
namespace EExprToken {
    // 基础运算
    const uint8 EX_LocalVariable       = 0x01;  // 读取局部变量
    const uint8 EX_InstanceVariable     = 0x02;  // 读取成员变量
    const uint8 EX_Assignment           = 0x03;  // 变量赋值
    const uint8 EX_Add_Int             = 0x04;  // 整数加法
    const uint8 EX_Subtract_Int        = 0x05;  // 整数减法
    const uint8 EX_CallMathFunction    = 0x06;  // 调用数学函数

    // 控制流
    const uint8 EX_Jump                = 0x07;  // 无条件跳转
    const uint8 EX_JumpIfNot           = 0x08;  // 条件跳转
    const uint8 EX_Return              = 0x09;  // 函数返回

    // 函数调用
    const uint8 EX_CallFunction        = 0x0A;  // 调用普通函数
    const uint8 EX_CallDelegate        = 0x0B;  // 调用委托

    // 对象操作
    const uint8 EX_SpawnActor          = 0x0C;  // 生成 Actor
    const uint8 EX_DestroyActor        = 0x0D;  // 销毁 Actor
    const uint8 EX_Array_GetByRef      = 0x0E;  // 数组按引用获取

    // 调试
    const uint8 EX_DebugBreak          = 0xFF;  // 断点
}
```

### 4.3 ExpandNode 示例：IfThenElse 展开

```cpp
// K2Node_IfThenElse 的 ExpandNode 实现逻辑
void UK2Node_IfThenElse::ExpandNode(
    FKismetCompilerContext& CompilerContext,
    UEdGraph* SourceGraph
) {
    // 1. 创建 Branch 节点（底层节点）
    UK2Node_Branch* BranchNode = CompilerContext.SpawnIntermediateNode<
        UK2Node_Branch>(this, SourceGraph);

    // 2. 把本节点的 Condition Pin 连接到 Branch 的 Condition
    CompilerContext.MovePinLinksToIntermediate(
        *FindPin(TEXT("Condition")),
        *BranchNode->FindPin(TEXT("Branch"))
    );

    // 3. 本节点的 True 分支连接到 Branch 的 True
    CompilerContext.MovePinLinksToIntermediate(
        *FindPin(TEXT("Then")),
        *BranchNode->FindPin(TEXT("True"))
    );

    // 4. 本节点的 False 分支连接到 Branch 的 False
    CompilerContext.MovePinLinksToIntermediate(
        *FindPin(TEXT("Else")),
        *BranchNode->FindPin(TEXT("False"))
    );

    // 5. 把 exec pin 连接到 Branch 的 exec
    UEdGraphPin* ExecPin = GetExecPin();
    UEdGraphPin* BranchExec = BranchNode->GetExecPin();
    ExecPin->MoveToIntermediate(BranchExec, ...);

    // 6. 断开本节点所有链接（已被展开）
    BreakAllNodeLinks();
}
```

---

## 5. 自定义 K2Node 实战

### 5.1 完整自定义节点：IsValid 判断

```cpp
// MyK2Node_IsValid.h
UCLASS()
class UMyK2Node_IsValid : public UK2Node {
    GENERATED_BODY()

public:
    // 节点标题
    virtual FText GetNodeTitle(ENodeTitleType::Type TitleType) const override {
        return LOCTEXT("IsValid", "Is Valid");
    }

    // 工具提示
    virtual FText GetTooltipText() const override {
        return LOCTEXT("Tooltip", "Checks if an object reference is valid");
    }

    // 创建默认引脚
    virtual void AllocateDefaultPins() override {
        Super::AllocateDefaultPins();

        // Exec 输入
        UEdGraphPin* ExecIn = CreatePin(
            EGPD_Input,
            UEdGraphSchema_K2::PC_Exec,
            FName(TEXT("Execute"))
        );

        // 对象输入
        UEdGraphPin* ObjectIn = CreatePin(
            EGPD_Input,
            UEdGraphSchema_K2::PC_Object,
            UObject::StaticClass(),
            FName(TEXT("Object"))
        );

        // Exec 输出 - Valid 分支
        UEdGraphPin* ValidOut = CreatePin(
            EGPD_Output,
            UEdGraphSchema_K2::PC_Exec,
            FName(TEXT("Is Valid"))
        );

        // Exec 输出 - Invalid 分支
        UEdGraphPin* InvalidOut = CreatePin(
            EGPD_Output,
            UEdGraphSchema_K2::PC_Exec,
            FName(TEXT("Is Not Valid"))
        );
    }

    // 核心！展开为本节点组合
    virtual void ExpandNode(
        FKismetCompilerContext& CompilerContext,
        UEdGraph* SourceGraph
    ) override {
        Super::ExpandNode(CompilerContext, SourceGraph);

        // 获取引脚
        UEdGraphPin* ObjectPin = FindPin(TEXT("Object"));
        UEdGraphPin* ValidOut = FindPin(TEXT("Is Valid"));
        UEdGraphPin* InvalidOut = FindPin(TEXT("Is Not Valid"));

        // 1. 创建 Branch 节点
        UK2Node_CallFunction* BranchNode =
            CompilerContext.SpawnIntermediateNode<UK2Node_CallFunction>(
                this, SourceGraph);

        // 2. 设置为 UKismetSystemLibrary::IsValid
        BranchNode->FunctionReference.SetExternalMember(
            GET_FUNCTION_NAME_CHECKED(UKismetSystemLibrary, IsValid),
            UKismetSystemLibrary::StaticClass()
        );
        BranchNode->AllocateDefaultPins();

        // 3. 迁移 Object 引脚
        CompilerContext.MovePinLinksToIntermediate(
            *ObjectPin,
            *BranchNode->FindPin(TEXT("Object"))
        );

        // 4. 迁移 Valid 分支
        CompilerContext.MovePinLinksToIntermediate(
            *ValidOut,
            *BranchNode->FindPin(TEXT("True"))
        );

        // 5. 迁移 Invalid 分支
        CompilerContext.MovePinLinksToIntermediate(
            *InvalidOut,
            *BranchNode->FindPin(TEXT("False"))
        );

        // 6. 断开本节点
        BreakAllNodeLinks();
    }

    // 节点类别（编辑器中显示在哪）
    virtual FName GetNodeCategory() const override {
        return FName(TEXT("Flow Control"));
    }
};
```

### 5.2 注册节点到编辑器

```cpp
// 在模块的 StartupModule() 中
void FMyModule::StartupModule() {
    // 注册节点（通常在 Editor 模式下）
#if WITH_EDITOR
    FKismetEditorUtilities::RegisterAutoGeneratedNode(
        UMyK2Node_IsValid::StaticClass(),
        &UMyK2Node_IsValid::CreateDefaultNode
    );
#endif
}
```

---

## 6. 执行模型

### 6.1 Blueprint VM 执行循环

编译后的字节码在 UE 的 Script 虚拟机上执行：

```cpp
// 简化自 Engine/Source/Runtime/Engine/Private/KismetVM/
void FKismetVM::ExecuteUbergraph(UObject* Object, int32 StartNode) {
    // 1. 获取编译后的 UFunction
    UFunction* Function = Object->FindFunctionChecked(FName("ExecuteUbergraph"));

    // 2. 创建 Stack + Context
    FFrame Stack(Function, Object, NULL);
    KismetExecutionContext Context(&Stack);

    // 3. 字节码解释循环
    while (true) {
        uint8 Opcode = *Stack.Code++;

        switch (Opcode) {
            case EX_Jump: {
                int32 Offset = ReadInt(&Stack);
                Stack.Code = Function->Script.GetData() + Offset;
                break;
            }
            case EX_JumpIfNot: {
                int32 Offset = ReadInt(&Stack);
                bool Condition = ReadBool(&Stack);
                if (!Condition) {
                    Stack.Code = Function->Script.GetData() + Offset;
                }
                break;
            }
            case EX_CallFunction: {
                // 读取函数引用，调用 C++ 实现
                FName FuncName = ReadName(&Stack);
                UFunction* TargetFunc = FindFunction(FuncName);
                Context.ProcessInternalCall(TargetFunc);
                break;
            }
            case EX_Return: {
                return;  // 函数结束
            }
            // ... 其他指令
        }
    }
}
```

### 6.2 循环在字节码中的实现

Blueprint 支持 For、While、ForEach 等循环。循环不是原生支持的，而是通过**跳转指令**实现：

```
// Blueprint 源码 (简化):
// for (int i = 0; i < 10; i++) { Print(i); }

// 编译后字节码:
0: EX_LocalVariable  i        // 声明 i
1: EX_Assignment    i = 0    // i = 0
2: EX_JumpIfNot     8        // 如果 i >= 10，跳到 8（循环结束）
3: Call   Print(i)           // 调用 Print(i)
4: EX_Add_Int       i, 1     // i = i + 1
5: EX_Assignment    i         // 写回 i
6: EX_Jump          2         // 跳回 2
7: (循环体结束)
8: (后续代码)
```

---

## 7. 蓝图类系统

### 7.1 Blueprint 继承层次

```
UBlueprint
  ├── UBlueprintGeneratedClass  // 编译产物
  │     ├── UActorComponent     // 组件
  │     ├── APawn              // 角色
  │     ├── ACharacter         // 角色（带 MovementComponent）
  │     └── AActor             // 所有可放置对象
  │
  └── UUserDefinedEnum         // 枚举
  └── UUserDefinedStruct       // 结构体
```

### 7.2 事件与委托

Blueprint 事件系统允许 C++ 定义事件，Blueprint 实现响应：

```cpp
// C++ 端声明事件
DECLARE_EVENT(AMyActor, FOnActorDestroyed)
FOnActorDestroyed OnActorDestroyed;

// C++ 端触发事件
void AMyActor::DestroyActor() {
    // ... 销毁逻辑
    OnActorDestroyed.Broadcast(this);
}

// Blueprint 端监听
// 在事件图表中：
// OnActorDestroyed (Event) → Print String "Actor was destroyed"
```

---

## 8. 与 C++ 的互操作

### 8.1 暴露 C++ 函数到 Blueprint

```cpp
// MyClass.h
UFUNCTION(BlueprintCallable, Category = "MyCategory")
float CalculateDistance(FVector A, FVector B);

// MyClass.cpp
float UMyClass::CalculateDistance(FVector A, FVector B) {
    return FVector::Dist(A, B);
}
```

### 8.2 暴露 C++ 变量到 Blueprint

```cpp
// C++ 端
UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Stats")
int32 Health;

// Blueprint 端可以直接 Get Health / Set Health
```

### 8.3 蓝图原生化（BlueprintPure）

```cpp
// BlueprintCallable + BlueprintPure = 无 exec pin 的纯节点
UFUNCTION(BlueprintCallable, BlueprintPure, Category = "Math")
static float Add(float A, float B) { return A + B; }
```

---

## 9. 优缺点分析

### 9.1 优点

1. **图灵完备 + 高性能**：编译为字节码，比解释执行快 10-100 倍
2. **与 C++ 完全互通**：任何 `UFUNCTION` / `UPROPERTY` 都可暴露
3. **强类型系统**：Pin 类型不匹配时编辑器直接拒绝连接
4. **完整的 OOP**：类、继承、接口、成员变量、构造逻辑
5. **工业级稳定性**：Epic 十年打磨，百万行代码游戏验证

### 9.2 缺点

1. **VM 老旧**：继承自 1998 年 UnrealScript，性能上限低于原生 C++
2. **图复杂度爆炸**：>100 节点的图几乎无法维护
3. **强耦合 UE**：无法独立使用
4. **编译时间**：大型蓝图编译可能需要数十秒
5. **调试困难**：节点级调试不够精细

---

## 10. 核心设计模式

### 10.1 模式一：ExpandNode 展开模式

所有复杂节点在编译时"展开"为简单节点组合。这是 Blueprint 的核心编译策略：

```
K2Node (编译期)
    ↓ ExpandNode()
简单节点组合 (运行时)
```

### 10.2 模式二：Functor 处理函数

每种节点类型配套一个 `FNodeHandlingFunctor`，负责把节点翻译为 IR：

```cpp
class FKCHandle_VariableSet : public FNodeHandlingFunctor {
    // 1. 注册本类型处理的节点
    virtual void RegisterNets(FKismetFunctionContext& Context,
                              UEdGraphNode* Node) override;

    // 2. 生成中间表示
    virtual void GenerateKOBSC(FKismetFunctionContext& Context,
                              UEdGraphNode* Node,
                              TArray<FBKOperator>& Ops) override;

    // 3. 编译为字节码
    virtual void Compile(FKismetFunctionContext& Context,
                         const FBKOperator& Op) override;
};
```

### 10.3 模式三：MemberReference 跨模块调用

```cpp
// 引用其他 Blueprint 的函数/变量
FMemberReference MemberReference;

// 编译时解析：
// 1. 查找目标 UBlueprintGeneratedClass
// 2. 查找目标 UFunction/UProperty
// 3. 生成跨类调用的字节码
```

---

## 11. 性能优化技巧

### 11.1 延迟执行

```cpp
// 在节点中延迟某些计算的执行
void UMyK2Node_HeavyCalculation::Execute(... {
    // 不要在主线程做重型计算
    AsyncTask(ENamedThreads::AnyBackgroundThreadSafetyNormalTask, [&]() {
        // 后台计算
        // ...
        // 结果通过 Event 回调
    });
});
```

### 11.2 缓存节点结果

```cpp
// 对于 Pure 节点，可以缓存输入
class UMyK2Node_CachedMath : public UK2Node {
    virtual bool IsNodePure() const override { return true; }

    // 缓存上次计算的输入和结果
    TPair<FVector, float> Cache;
};
```

---

## 12. 推荐阅读与资源

### 官方资料
- [Blueprints Visual Scripting System](https://dev.epicgames.com/documentation/en-us/unreal-engine/blueprints-visual-scripting-in-unreal-engine)
- [K2Node Documentation](https://dev.epicgames.com/documentation/en-us/unreal-engine/blueprint-kismet-nodes-in-unreal-engine)

### 深度博客
- **Intax - Discovering Blueprint VM** (intaxwashere.github.io) - VM 内部机制
- **JaydenGames - From Blueprint to Bytecode** (jaydengames.com) - 编译管线
- **Gamedev Guide - Anatomy of the Unreal 4 Blueprint VM** (ikrima.dev)

### 源码位置
```
Engine/Source/Editor/BlueprintGraph/     # K2Node 定义
Engine/Source/Runtime/Engine/Private/KismetVM/  # VM 执行
Engine/Source/Editor/Kismet/            # 编辑器界面
```

---

## 13. 小结

Unreal Blueprint 是**工业级可视化脚本的标杆**，其设计精髓：

1. **ExpandNode 展开模式**：复杂节点编译时展开为简单组合
2. **双 Pin 系统**：exec（控制流）+ data（数据流）分离
3. **编译为字节码**：高性能执行，而非解释执行
4. **Pure vs Impure**：数据流与控制流的哲学分离
5. **与 C++ 无缝互通**：任何 `UFUNCTION` 即可暴露

理解 Blueprint 的编译管线是学习"如何设计可视化编程语言"的最佳实践。

---

## 14. 源码深度解析

> 本节从 Unreal Engine 公开源码（`Engine/Source/...`）和 Epic 官方编译器文档出发，结合 Tim Sweeney 1998 年遗产的 UnrealScript VM 演化路径，给出 Blueprint 编译器与虚拟机的"代码级"剖面。Epic 仓库需 GitHub-Epic 账号绑定才可访问，因此下文部分代码来自 Epic 官方文档、`ikrima.dev`（前 Epic 工程师维护的引擎笔记）、Intax 的 Blueprint VM 系列博客、Jayden 的 *From Blueprint to Bytecode* 系列等公开转引。**凡是未在公开文档中逐字给出的片段，均标注"近似复现，未直接验证"**；其余为可与 4.27 / 5.x 头文件对照的直接引用。

### 14.1 类层级与文件分布

Blueprint 子系统横跨编辑器（`Editor/`）和运行时（`Runtime/CoreUObject/`）两侧。核心类型与其源码路径如下：

| 类型 | 角色 | 源码位置（基于 5.x） |
|------|------|----------------------|
| `UBlueprint` | 资产层（保存到 .uasset） | `Engine/Source/Runtime/Engine/Classes/Engine/Blueprint.h` |
| `UBlueprintGeneratedClass` | 编译后的 UClass，挂载 UFunction | `Engine/Source/Runtime/Engine/Classes/Engine/BlueprintGeneratedClass.h` |
| `UEdGraph` / `UEdGraphNode` / `UEdGraphPin` | 通用编辑器图模型 | `Engine/Source/Runtime/Engine/Classes/EdGraph/` |
| `UK2Node` 及子类 | Kismet2（蓝图）专用节点 | `Engine/Source/Editor/BlueprintGraph/Classes/` |
| `FKismetCompilerContext` | 一次编译的容器，负责 Pipeline 调度 | `Engine/Source/Editor/KismetCompiler/Public/KismetCompiler.h` |
| `FBlueprintCompiledStatement` | 编译中间表示（线性 IR） | `Engine/Source/Editor/KismetCompiler/Public/BlueprintCompiledStatement.h` |
| `FKismetCompilerVMBackend` | IR → Bytecode 后端 | `Engine/Source/Editor/KismetCompiler/Private/KismetCompilerVMBackend.{h,cpp}` |
| `UFunction`（含 `Script: TArray<uint8>`） | 运行时函数，字节码挂在这里 | `Engine/Source/Runtime/CoreUObject/Public/UObject/Class.h` |
| `FFrame` | VM 栈帧 | `Engine/Source/Runtime/CoreUObject/Public/UObject/Stack.h` |
| `EExprToken`（字节码枚举） | 100+ 操作码 | `Engine/Source/Runtime/CoreUObject/Public/UObject/Script.h` |
| `ScriptCore.cpp` | VM 执行循环 | `Engine/Source/Runtime/CoreUObject/Private/UObject/ScriptCore.cpp` |

继承上：`UEdGraphNode → UK2Node → UK2Node_IfThenElse / UK2Node_CallFunction / ...`；`UClass → UBlueprintGeneratedClass`；`UBlueprint` 本身**不**继承 `UClass`，它是描述资产的 `UObject`，编译产物 `UBlueprintGeneratedClass` 才是真正的运行时类。

### 14.2 字节码操作码 `EExprToken`（直接引用自 `Script.h`）

```cpp
// Engine/Source/Runtime/CoreUObject/Public/UObject/Script.h
enum EExprToken
{
    EX_LocalVariable        = 0x00, // A local variable.
    EX_InstanceVariable     = 0x01, // An object variable.
    EX_DefaultVariable      = 0x02, // Default variable for a class context.
    EX_Return               = 0x04, // Return from function.
    EX_Jump                 = 0x06, // Goto a local address in code.
    EX_JumpIfNot            = 0x07, // Goto if not expression.
    EX_Let                  = 0x0F, // Assign an arbitrary size value to a variable.
    EX_LetBool              = 0x14,
    EX_EndFunctionParms     = 0x16,
    EX_Self                 = 0x17,
    EX_Context              = 0x19, // Call a function through an object context.
    EX_VirtualFunction      = 0x1B, // Virtual UFunction call (looked up by name).
    EX_FinalFunction        = 0x1C, // Direct UFunction call (compiled-in pointer).
    EX_IntConst             = 0x1D,
    EX_FloatConst           = 0x1E,
    EX_True                 = 0x27,
    EX_False                = 0x28,
    EX_LetObj               = 0x59,
    EX_CallMath             = 0x68, // Direct call into a static native math function.
    EX_EndOfScript          = 0xFF,
    EX_Max                  = 0x100,
};
```

> 注释中文为本节添加；UE 原注释为英文。完整枚举超过 90 项。

`UFunction::Script` 就是 `TArray<uint8>`，里面是 `EExprToken` 序列加上紧随其后的 inline 数据（指针、整型、跳转偏移）。Intax 把旁边的 `ScriptSerialization.h` 称作 *"the most evil and scary file"*：opcode 与其参数体的内存布局都是手写 macro 控制的。

### 14.3 编译流水线（`FKismetCompilerContext::Compile`）

Epic 官方 *Compiler Overview* 给出 11 步编译顺序，对应 `KismetCompiler.h` 中的方法：

```cpp
// Engine/Source/Editor/KismetCompiler/Public/KismetCompiler.h
// 接口签名近似复现，未直接验证；功能注释按 Epic 官方文档排序
class KISMETCOMPILER_API FKismetCompilerContext : public FGraphCompilerContext
{
public:
    virtual void Compile();             // 入口

protected:
    virtual void CleanAndSanitizeClass(UBlueprintGeneratedClass* ClassToClean,
                                       UObject*& InOutOldCDO);                  // 1
    virtual void CreateClassVariablesFromBlueprint();                           // 2
    virtual void CreateFunctionList();                                          // 3
    virtual void CreateAndProcessUberGraph();                                   //   3a
    virtual void ProcessOneFunctionGraph(UEdGraph* SourceGraph,
                                         bool bInternalFunction = false);       //   3b
    virtual void PrecompileFunction(FKismetFunctionContext& Context,
                                    EInternalCompilerFlags InternalFlags);      // 4
    virtual void CompileClassLayout(EInternalCompilerFlags InternalFlags);      // 5  类布局阶段
    virtual void CompileFunctions(EInternalCompilerFlags InternalFlags);        // 6  函数阶段
    virtual void CompileFunction(FKismetFunctionContext& Context);              //   6a
    virtual void PostcompileFunction(FKismetFunctionContext& Context);          //   6b
    virtual void FinishCompilingClass(UClass* Class);                           // 7
};
```

各阶段语义：

1. **CleanAndSanitizeClass**：把旧的属性、函数搬到 transient package 的"垃圾类"里，再清空当前 `UBlueprintGeneratedClass`。
2. **CreateClassVariablesFromBlueprint**：把 `FBPVariableDescription` 转成真正的 `FProperty` 挂到类上。
3. **CreateAndProcessUberGraph / ProcessOneFunctionGraph**：合并所有事件图为 *UberGraph*（蓝图断点会落在 `ExecuteUbergraph_<BPName>`）；这一步会**调用每个 K2Node 的 `ExpandNode`**——宏节点、`ForEachLoop`、`IfThenElse` 在此被降低为更原始的中间节点。
4. **PrecompileFunction**：注册网（`RegisterNets`），构建线性执行列表，把图依赖压平成指令序列。
5. **CompileClassLayout**：绑定（Bind）和链接（Link）`UClass`。
6. **CompileFunctions / CompileFunction**：逐个调用节点处理器（`FNodeHandlingFunctor::Compile`）追加 IR 语句；`KCST_*` 是 IR opcode：`KCST_Assignment`、`KCST_CallFunction`、`KCST_GotoIfNot`、`KCST_Return`、`KCST_GotoReturn` ……
7. **FinishCompilingClass + Backend**：选择 `FKismetCompilerVMBackend`（默认）或 `FKismetCppBackend`（Nativization）。

### 14.4 节点处理器与 IR 追加（直接引用片段）

```cpp
// 直接引用：FunctionResult 节点的 IR 生成
virtual void Compile(FKismetFunctionContext& Context, UEdGraphNode* Node) override
{
    GenerateAssigments(Context, Node);
    // Always go to return
    FBlueprintCompiledStatement& GotoStatement = Context.AppendStatementForNode(Node);
    GotoStatement.Type = KCST_GotoReturn;
}
```

```cpp
// 直接引用：赋值语句的标准三件套
FBlueprintCompiledStatement& Statement = Context.AppendStatementForNode(Node);
Statement.Type = KCST_Assignment;
Statement.LHS  = DstTerm;
Statement.RHS.Add(RHSTerm);
```

```cpp
// 直接引用：FKismetCompilerVMBackend::ConstructFunction 末尾的 Return
void FKismetCompilerVMBackend::ConstructFunction(
    FKismetFunctionContext& FunctionContext,
    bool bIsUbergraph,
    bool bGenerateStubOnly)
{
    // ...
    FBlueprintCompiledStatement ReturnStatement;
    ReturnStatement.Type = KCST_Return;
    ScriptWriter.GenerateCodeForStatement(CompilerContext, FunctionContext,
                                          ReturnStatement, /*Source*/ nullptr);
}
```

中文要点：节点处理器把图语义铺成 `FBlueprintCompiledStatement`（`KCST_*`）；后端 `FScriptBuilderBase::GenerateCodeForStatement` 再把每个 `KCST_*` 翻译成具体的 `EX_*` opcode 序列写入 `UFunction::Script`。例如 `KCST_Assignment` + 目标为 bool → `EX_LetBool`；目标为 UObject 指针 → `EX_LetObj`。`KCST_GotoIfNot` 直接对应 `EX_JumpIfNot + 偏移`，回填则在所有语句生成完后做一遍 patch。

### 14.5 `K2Node_IfThenElse` 的 ExpandNode 模式

`UK2Node_IfThenElse` **本身不实现 `ExpandNode`**——它直接对应一条 IR 语句 `KCST_GotoIfNot`，由专用的 `FKCHandler_Branch::Compile` 负责生成 IR：

```cpp
// 近似复现，未直接验证（基于 ikrima.dev 描述与公开范式）
void FKCHandler_Branch::Compile(FKismetFunctionContext& Context, UEdGraphNode* Node)
{
    UK2Node_IfThenElse* BranchNode = CastChecked<UK2Node_IfThenElse>(Node);
    UEdGraphPin* ConditionPin = BranchNode->GetConditionPin();
    UEdGraphPin* ElsePin      = BranchNode->GetElsePin();

    FBPTerminal** CondTerm = Context.NetMap.Find(FEdGraphUtilities::GetNetFromPin(ConditionPin));

    // 1) GotoIfNot Condition -> Else 分支
    FBlueprintCompiledStatement& IfNotStatement = Context.AppendStatementForNode(Node);
    IfNotStatement.Type = KCST_GotoIfNot;
    IfNotStatement.LHS  = *CondTerm;
    Context.GotoFixupRequestMap.Add(&IfNotStatement, ElsePin);

    // 2) 紧接落入 Then 分支（无条件 fall-through 由后续节点 emit）
    //    Else / Then 的实际地址在 PostCompileFunction 阶段回填
}
```

**ExpandNode 真正用得多的场景**是宏与高阶节点。例如自定义 `ForEach`、`UK2Node_VariableGet` 在 `ExpandNode` 内部 `SpawnIntermediateNode<UK2Node_IfThenElse>(this, SourceGraph)` 临时铺设一个 IfThenElse，然后用 `MovePinLinksToIntermediate` 重连。这就是 Blueprint *macro lowering* 的核心模式。

### 14.6 VM 执行：`FFrame` 与 `GNatives` 跳转表

```cpp
// Engine/Source/Runtime/CoreUObject/Public/UObject/Stack.h
struct FFrame : public FOutputDevice
{
    UFunction*           Node;          // 当前函数（含 Script: TArray<uint8>）
    UObject*             Object;        // 调用者（this）
    uint8*               Code;          // 指令指针，指向 Script 数据
    uint8*               Locals;        // 本地变量起始
    FProperty*           MostRecentProperty;
    uint8*               MostRecentPropertyAddress;
    FlowStackType        FlowStack;     // 跳转栈
    FFrame*              PreviousFrame; // 链式栈帧
    // ...
};
```

VM 主循环——这是 Blueprint VM **最关键的 4 行**，自 1998 年 UnrealScript 起几乎未变：

```cpp
// Engine/Source/Runtime/CoreUObject/Private/UObject/ScriptCore.cpp（直接引用）
void FFrame::Step(UObject* Context, RESULT_DECL)
{
    int32 B = *Code++;                                     // 读 1 字节 opcode
    (Context->*GNatives[B])(*this, RESULT_PARAM);          // 成员函数指针分派
}
```

注意它**不是**传统 `switch (op) { case ... }` 解释器，而是 C++ 成员函数指针表：

```cpp
typedef void (UObject::*Native)(FFrame& TheStack, RESULT_DECL);
Native GNatives[EX_Max];

// 每条 opcode 对应一个 execXxx 成员函数：
DEFINE_FUNCTION(UObject::execLetBool)   { /* ... */ }
DEFINE_FUNCTION(UObject::execLetObj)    { /* ... */ }
IMPLEMENT_VM_FUNCTION(EX_LetBool, execLetBool);
IMPLEMENT_VM_FUNCTION(EX_LetObj,  execLetObj);
```

C++ 进入字节码的入口是 `UObject::ProcessEvent`（自动生成的 thunk 把参数打包进 `FFrame`），跨函数调用走 `ProcessScriptFunction`，最里层的 native 调用走 `ProcessInternal`：

```cpp
// 近似复现，未直接验证
void UObject::ProcessInternal(FFrame& Stack, RESULT_DECL)
{
    UFunction* Function = (UFunction*)Stack.Node;
    uint8* Frame = (uint8*)FMemory_Alloca(Function->PropertiesSize);
    FMemory::Memzero(Frame, Function->PropertiesSize);

    FFrame NewStack(this, Function, Frame, &Stack, Function->ChildProperties);

    // 解析参数：循环 Step 直到遇到 EX_EndFunctionParms
    for (FProperty* Prop = (FProperty*)Function->ChildProperties;
         *Stack.Code != EX_EndFunctionParms;
         Prop = (FProperty*)Prop->Next)
    {
        Stack.Step(Stack.Object, NewStack.Locals + Prop->GetOffset_ForUFunction());
    }
    Stack.Code++; // 跳过 EX_EndFunctionParms

    // 执行函数体：循环 Step 直到 EX_Return
    while (*NewStack.Code != EX_Return)
        NewStack.Step(NewStack.Object, /*Result=*/nullptr);

    // 写回返回值
    NewStack.Step(NewStack.Object, RESULT_PARAM);
}
```

性能含义：每条 `EX_*` 都要走一次 `Code++` + 间接成员函数指针调用 + `FFrame` 构建——这就是为什么"在蓝图里写紧密循环"性能远逊于 C++。Epic 引入 `EX_CallMath`（直接静态调度，跳过虚表）就是针对这个瓶颈的优化。

### 14.7 串起来：一段 IfThenElse 的全链路

```
// IR (FBlueprintCompiledStatement 序列)
[0] KCST_GotoIfNot   LHS=Cond                 -> ElseLabel
[1] KCST_CallFunction Func=SetActorLocation   args=...
[2] KCST_UnconditionalGoto                    -> EndLabel
[3] KCST_Nop          (ElseLabel)
[4] KCST_Nop          (EndLabel)
[5] KCST_Return

// Bytecode (UFunction::Script)
EX_JumpIfNot  <offsetToElse>  EX_LocalVariable <CondProp>
EX_Context    <SelfObj>       EX_FinalFunction <SetActorLocation*>
                              <param payload> EX_EndFunctionParms
EX_Jump       <offsetToEnd>
... <ElseLabel> ...
... <EndLabel> ...
EX_Return     EX_Nothing
EX_EndOfScript
```

执行时 `FFrame::Step` 逐字节读入 `EX_JumpIfNot`，调用 `UObject::execJumpIfNot`，先读条件表达式（再次 Step → `execLocalVariable`），按结果改 `Code` 指针，循环回到 `Step`。整个过程没有传统 VM 的解码-分派 `switch`——一切都是函数指针表 + inline 数据流。

### 14.8 阅读路径建议

1. `Script.h` 的 `EExprToken` 全表（操作码地图）
2. `Stack.h` + `ScriptCore.cpp::FFrame::Step` 与 `DEFINE_FUNCTION(UObject::exec*)`（VM 执行）
3. `KismetCompiler.h::FKismetCompilerContext::Compile`（编译器入口）
4. `KismetCompilerMisc.cpp` 中各 `FKCHandler_*::Compile`（节点 → IR）
5. `KismetCompilerVMBackend.cpp::FScriptBuilderBase::GenerateCodeForStatement`（IR → Bytecode）
6. `BlueprintGraph/Classes/K2Node_*.h` —— 看若干 `ExpandNode` 实现学 macro lowering 模式

> 来源声明：本节字节码与 VM 核心片段引自 ikrima.dev、Intax、Jayden Games 系列博客及 Epic 官方文档；编译流水线步骤名与顺序与 Epic 官方一致。**未直接验证的代码块已就近标注**——若需 100% 校对，请绑定 Epic GitHub 账号后比对 `release` 分支对应文件。
