/**
 * `@opendesign/graph` 的公共入口。
 *
 * - 类型定义：见 {@link ./types}
 * - 运行时基础数据元模型 (Node、Edge、Graph、Socket 等)：见 {@link ./classic}
 * - 算法：见 {@link ./algorithms}
 * - 序列化：见 {@link ./serialize}
 * - petgraph 风格的 trait 别名：见 {@link ./traits}
 */

export * from './types';
export * from './algorithms';
export * from './classic';
export * from './serialize';
