{
  "name": "@openconsole/tunnel",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "check": "tsc --noEmit && vitest run"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.3",
    "hono": "^4.12.12"
  }
}
