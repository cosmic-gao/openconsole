{
  "$schema": "./node_modules/nx/schemas/nx-schema.json",
  "defaultBase": "main",
  "namedInputs": {
    "sharedGlobals": [
      {
        "runtime": "node --version"
      },
      "{workspaceRoot}/pnpm-lock.yaml",
      "{workspaceRoot}/pnpm-workspace.yaml",
      "{workspaceRoot}/.npmrc",
      "{workspaceRoot}/configs/tsconfig/**/*"
    ],
    "default": ["{projectRoot}/**/*", "sharedGlobals"],
    "production": [
      "default",
      "!{projectRoot}/**/*.{spec,test}.{ts,tsx}",
      "!{projectRoot}/tests/**/*",
      "!{projectRoot}/vitest.config.{ts,js,mjs}",
      "!{projectRoot}/tsconfig.tsbuildinfo"
    ]
  },
  "targetDefaults": {
    "typecheck": {
      "cache": true,
      "inputs": ["default", "^production"],
      "outputs": ["{projectRoot}/tsconfig.tsbuildinfo"],
      "dependsOn": ["^typecheck"]
    },
    "test": {
      "cache": true,
      "inputs": ["default", "^production"],
      "outputs": ["{workspaceRoot}/coverage/{projectName}"],
      "dependsOn": ["^typecheck"]
    },
    "build": {
      "cache": true,
      "inputs": ["production", "^production"],
      "outputs": ["{projectRoot}/dist"],
      "dependsOn": ["^build"]
    },
    "lint": {
      "cache": true,
      "inputs": ["default", "{workspaceRoot}/eslint.config.{js,cjs,mjs,ts}"]
    },
    "nx-release-publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm publish --no-git-checks --access public",
        "cwd": "{projectRoot}"
      }
    }
  },
  "release": {
    "projects": [
      "@openconsole/shadcn",
      "@openconsole/atoms",
      "@openconsole/nacos"
    ],
    "projectsRelationship": "independent",
    "releaseTag": {
      "pattern": "{projectName}@{version}"
    },
    "version": {
      "conventionalCommits": false,
      "specifierSource": "prompt",
      "preVersionCommand": "pnpm nx run-many -t typecheck test"
    },
    "changelog": {
      "workspaceChangelog": false,
      "projectChangelogs": true
    },
    "git": {
      "commit": true,
      "commitMessage": "chore(release): publish {version}",
      "tag": true,
      "push": true
    }
  },
  "analytics": true
}
